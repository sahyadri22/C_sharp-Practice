using System;
using System.Collections.Generic;
using System.Linq;
namespace Hello
{
  class Program
  {
    static void Main( )
    {
      List<int>no=new List<int>(){1,2,3,4,5,1,3,4};
      HashSet<int> unique= no.ToHashSet(); /*here we converted a list 
                                            to a hashset using ToHashSet method*/
      HashSet<int> num= new HashSet<int>(no);/*here we directly put elemenets
                                              in list as element in hasset*/
      
      foreach (int el in unique)
      {
      Console.WriteLine(el);
      }
    Console.WriteLine( );
      foreach(int el in no)
      {
       Console.WriteLine(el) ;
      }
      Console.WriteLine( );
      foreach(int el in num)
      {
        Console.WriteLine(el);
      }
    }  
      
  }
}